# 2026-09 update log

Index and query commands: [README.md](README.md). New entries go at the end.

---

## U-20260922-01 · 2026-09-22 · Adopt progress/architecture/docs-updates rules · #docs #migration

- **What**: `CLAUDE.md` gained the workspace rule section (commit at every stage; `progress.md` holds open items only; finished work is recorded in `docs/updates/`; `architecture.md` is the short overview). Added `architecture.md` and this `docs/updates/` index. `progress.md` starts with the open items found in the workspace inventory of 2026-09-22 (`D:\Codes\docs\updates\2026-09.md`, U-20260922-01..05). `docs/conf.py` now excludes `updates` from the Sphinx build.
- **Files**: `CLAUDE.md`, `progress.md`, `architecture.md`, `docs/conf.py`, `docs/updates/`.
- **Open items**: see `progress.md`.

## U-20260922-02 · 2026-09-22 · Stop tracking .idea/ · #done #housekeeping

- **What**: removed the JetBrains settings under `.idea/` from version control with `git rm -r --cached .idea` (8 files); the files stay on disk. Workspace item W-4 (`D:\Codes\progress.md`). Also closes `progress.md` #4.
- **Files**: `.idea/` (untracked); `.gitignore` already ignored `.idea/`.
- **Open items**: none.

## U-20260922-03 · 2026-09-22 · Commit the PyPI publish workflow and setuptools bump · #done #release

- **What**: committed the long-uncommitted `.github/workflows/publish.yml` and the build-system bump from `setuptools>=61.0` to `setuptools>=82.0.1` in `pyproject.toml` and `dev.toml` (work from 2026-04-21..25). The workflow runs on pushes to `main` (skipped when the head commit says `[skip publish]`): it bumps the patch version in `pyproject.toml`, commits and tags it, builds sdist and wheel, checks them with `twine check`, uploads to PyPI with the `PYPI_API_TOKEN` secret and creates a GitHub release. Closes `progress.md` #1.
- **Result / numbers**: both TOML files and the workflow YAML parse. Nothing is published until `dev` is merged to `main`; PyPI stays at 0.1.33 until then.
- **Files**: `.github/workflows/publish.yml`, `pyproject.toml`, `dev.toml`, `progress.md`.
- **Open items**: none.

## U-20260923-01 · 2026-09-23 · The step log is written as UTF-8 · #done #logging

- **What**: `TestPioneerHandler` (`test_pioneer/logging/loggin_instance.py`) opened the `pioneer_log` file named in the user's YAML with the locale encoding. Step names and messages come from that YAML, so on a zh-TW Windows console (cp950) any record with a character outside the code page raised inside `emit()` and was dropped. The handler now writes UTF-8 with `errors="backslashreplace"`, so a lone surrogate from a Windows path is escaped instead of losing the record.
- **Checked while here** (workspace X-6): TestPioneer does not write a log into the working directory at import — it only adds this handler when a YAML file names `pioneer_log`, which is a location the user chose — so it needs no move.
- **Tests**: `test/test_logging.py` +1: a record with CJK, a character outside cp950, an emoji and a lone surrogate is written and reads back as UTF-8 (fails on the old handler). Suite: 110 passed.
- **Files**: `test_pioneer/logging/loggin_instance.py`, `test/test_logging.py`.
- **Open items**: none.

## U-20260923-02 · 2026-09-23 · run_folder works for the web, api and load runners · #done #bugfix

- **Bug** (`progress.md` #5, was [UNVERIFIED]; confirmed): a `run_folder` step handed the runner the list of JSON files in the folder, but for `web-runner`, `api-runner` and `load-runner` the runner was each framework's `execute_action`, which takes one action list, so every file path was rejected as a malformed action ("executor received invalid data") and nothing in the folder ran. Only `gui-runner` was mapped to `execute_files`.
- **Fix**: in `run_folder` mode all three map to their framework's `execute_files` (each exports it at top level); `run_folder` passes the files as sorted string paths. Also, AutoControl is now imported only for a `gui-runner` step: it was imported for every step whenever `je_auto_control` was installed, and where it is installed without its Qt dependencies (as in this repository's `.venv`) any web/api/load step failed at import.
- **Tests**: `test/test_run_utils.py` +2 (`run` resolves each runner's `execute_action`, `run_folder` its `execute_files`); `test/test_run_folder_files.py` (2): the runner receives the folder's `.json` files as sorted strings, and end to end with LoadDensity's executor both files' actions run. Three of these fail on the old code. Suite: 114 passed.
- **Files**: `test_pioneer/executor/run/utils.py`, `test_pioneer/executor/run/executor_run_folder.py`, `test/test_run_utils.py`, `test/test_run_folder_files.py`, `progress.md`.
- **Open items**: none.

## U-20260923-03 · 2026-09-23 · file-runner: FileAutomation joins run, run_folder and parallel_run · #done #feature

- **What** (`progress.md` #6): `automation-file` is a core dependency and FileAutomation keeps the same legacy CLI as the other frameworks (`--execute_file`, guarded by its contract test), but no runner tag reached it: `parallel_run` knew web, api, load and gui only, and so did `run`/`run_folder`. A `file-runner` tag now maps to `automation_file` everywhere — `execute_action` for `run`, `execute_files` for `run_folder`, and `python -m automation_file --execute_file <script>` for `parallel_run`.
- **Tests**: `file-runner` resolves `automation_file.execute_action` / `execute_files` (`test/test_run_utils.py`), and `parallel_run` spawns `-m automation_file --execute_file <resolved script>` (`test/test_parallel_run.py`). Suite: 116 passed.
- **Docs**: README and `docs/runners.rst` list the runner, `docs/step-types.rst` names it among the `with:` values, and `architecture.md` §3/§6 lists the new import and subprocess target (and no longer says `run_folder` misroutes; that was fixed in U-20260923-02).
- **Files**: `test_pioneer/executor/run/{utils,parallel_run}.py`, `test/test_run_utils.py`, `test/test_parallel_run.py`, `README.md`, `docs/runners.rst`, `docs/step-types.rst`, `architecture.md`, `progress.md`.
- **Open items**: none.
